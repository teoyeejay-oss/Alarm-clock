# Alarm Clock

A Python project that plays a music file when the set alarm time is reached.

## Features
- Set alarm time in HH:MM format
- Checks current time every second
- Plays a music file when alarm triggers

## Concepts Used
- `datetime` module
- `time` module
- `playsound` library
